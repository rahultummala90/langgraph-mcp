{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "CMD=$(jq -r '.tool_input.command'); echo \"[hook] command=$CMD\" >> /tmp/hook-debug.txt; echo \"$CMD\" | grep -q '^git push' && echo \"[hook] git push detected\" >> /tmp/hook-debug.txt || true"
          }
        ]
      }
    ]
  }
}
